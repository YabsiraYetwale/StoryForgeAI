# StoryForge AI - ZERO COST MODE (no API keys needed)
# Copy to .env only if you want to override defaults. You can run without .env.

# --- Scene planning (free options) ---
# Leave OPENAI_API_KEY empty = paragraph-based scene split (no AI, no cost)
# Optional FREE: use local LLM (e.g. Ollama) - no key, no money:
# OPENAI_API_BASE=http://localhost:11434/v1
# SCENE_MODEL=llama3.2
# (Install Ollama from https://ollama.com and run: ollama run llama3.2)

# OPENAI_API_KEY=

# --- Image generation (ZERO MONEY = huggingface only) ---
# "placeholder" = text-only cards, no real images (default)
IMAGE_BACKEND=placeholder
# ZERO COST: real images (girl, scene) with local Stable Diffusion - no API key, no money:
# IMAGE_BACKEND=huggingface
# (Requires: pip install -r python_service/requirements.txt . GPU is faster but CPU works.)
# --- Paid options (not zero cost): openai (DALL-E), replicate (need API keys) ---

# --- Narration & video: always free (edge-tts, MoviePy, FFmpeg) ---
